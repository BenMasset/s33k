# s33k: Build Plan

**What s33k is:** one open, self-hosted tool that a marketer controls entirely from their LLM over MCP, combining three things they check constantly:
- **SEO:** where every page ranks in Google for its target keywords (SerpBear core).
- **Analytics:** page traffic and sources (start by reading Lodd's API; later replace Lodd with self-hosted Umami).
- **AEO:** whether ChatGPT, Claude, Perplexity, and Gemini mention and cite the brand vs competitors (built natively on top of the LLM connection).

The product is the **unified MCP control plane** that joins all three. Forked from `towfiqi/serpbear` (MIT). Domain: s33k.io.

**Documented up front:** the AI assistant assessed this as a multi-week product and recommended a scoped proof of concept; Ben overruled it and committed to a rough, installable V1 inside Fork Week. This plan is how we try.

---

## Current State Note

This file is a historical build log with older milestone counts preserved in place. The current MCP
state is: 81 tools and 5 knowledge resources. The authoritative tool registry is
`mcp/src/tools.ts` (`registerS33kTools`), shared by the stdio entry `mcp/src/index.ts` and the hosted
HTTP endpoint `pages/api/mcp/[[...slug]].ts`. Current AI handoff and improvement status lives in
`AGENTS.md`, `CLAUDE.md`, and `S33K_IMPROVEMENT_AUDIT.md`; read those before treating an older
milestone below as current.

---

## Guiding principle: 5 minutes to value

A friend should get from install to seeing real data in about five minutes. This is the bar every onboarding decision is measured against. Implication for data sources:
- **Serper is the day-one value path.** One API key, paste it, add keywords, see live rankings in ~2 minutes. This is what onboarding leads with.
- **Google Search Console is the richer opt-in layer, not the first step.** SerpBear's GSC connection is a service-account flow (create a Google Cloud project, service account, download a JSON key, paste client_email + private_key, add as a user in GSC): a 15-30 minute slog that breaks the 5-minute promise. So GSC is "level 2," connected after first value.
- **Build a one-click "Connect Google Search Console" OAuth** to replace the slog. SerpBear lacks this; it is a real differentiator and a strong upstream PR. Self-hosted wrinkle (OAuth redirect URIs) solved with a device-code flow or a small hosted auth broker. Roadmap item, not day one.

## Phase 0: Foundation (Day 3)
- [x] Fork SerpBear to `BenMasset/s33k`, name it, register s33k.io
- [x] Create local `.env`, boot locally (Node 20 pinned; runs on port 3005)
- [x] Install deps and boot s33k locally (login works: admin / local placeholder)
- [x] Wire **Serper** as the SERP source (Ben added his key; verified working)
- [x] Add the starter keyword set, each mapped to its target page (target page parked in the `tags` field until Phase 1)
- [x] First live Google rankings on the board (getmasset.com: "masset" #1, "Seismic alternative" #38, the other 5 are opportunity gaps not yet in top 100). Scrape ran in ~11s.
- [ ] (Optional, level 2) Connect Google Search Console for real impression data

## Phase 1: Per-page mapping (Day 4). DONE
- [x] Add a `target_page` field to keywords (model + migration + API + types + parseKeywords). Existing keywords backfilled from tags.
- [x] Show target page in the keyword list (Target Page column) and an optional Target Page input in the Add-Keyword form. (Full per-page grouping view can come later; the column is in.)

## Phase 1.5: Kill the GSC friction (the 5-min-to-value differentiator)
- [ ] Replace the GSC service-account paste with a one-click "Connect Google Search Console" OAuth button
- [ ] Handle the self-hosted redirect-URI problem (device-code flow or a small hosted auth broker)
- [ ] Shape it as an upstream pull request to SerpBear

## Phase 2: The MCP control layer (Day 5, the headline). DONE
- [x] Built an MCP server (`mcp/`) over s33k's REST API. Verified returning live data over the MCP protocol.
- [x] **MCP completeness: the whole product is now controllable from MCP with no UI (V1 surface).** 20 tools registered and verified over stdio (`tools/list` + live `tools/call`):
  - SEO/control: list_domains, create_domain, list_keywords, add_keyword, update_keyword, delete_keyword, refresh_keywords, get_insight.
  - Onboarding: discover_pages (crawl a domain, return the live page list so keywords map to real pages in one shot).
  - Analytics (full Lodd parity + extras): page_scoreboard, ai_referrals, traffic_summary, traffic_breakdown, traffic_timeseries, top_events, engagement.
  - 10x signals (beyond commodity analytics): human_traffic (bot-vs-human filtering), insights (cross-pillar SEO + analytics + AEO findings). (An answer-engine bot-detection signal was built and later HIDDEN, see branch `hide-ai-crawler-feature`.)
  - Proactive analyst: briefing (a single cross-pillar daily standup, see the Daily Briefing section below).
- [x] verifyUser whitelist extended so every headless tool works with the Bearer API key (added PUT/DELETE keywords + the 5 new analytics GET routes). README has the Claude Code connect command. (Ben runs `claude mcp add` to wire it in.)
- [x] **End-to-end smoke test over the real MCP transport: 20/20 tools verified.** `mcp/smoke-test.mjs` (run with `npm run smoke` from `mcp/`) spawns the BUILT `dist/index.js` as a stdio child, drives it with the official MCP client SDK (real `initialize` handshake), asserts exactly 20 tools are registered, and `tools/call`s every one against the live API (port 3005). Result: 22/22 assertions pass (handshake + tools/list + all 20 tools), exit 0. Read tools hit real getmasset.com data read-only; the four mutating tools run only against a throwaway `s33k-smoke-test.example` domain. The harness is idempotent: it deletes the throwaway domain before and after the mutation block via `DELETE /api/domains`. To support that cleanup, `DELETE:/api/domains` was added to the API-key whitelist in `utils/verifyUser.ts` (the API could create domains but not delete them, a real create-but-cannot-delete gap; one-line fix, no new MCP tool, tool count stays 20). `get_insight` correctly reports "Google Search Console is not Integrated" (GSC not wired) and is counted as a pass because the tool behaved correctly.
- [ ] Stretch: shape the MCP server as the upstream pull request to SerpBear
- [ ] Follow-up: `get_insight` needs Search Console connected (Phase 1.5), not yet wired

## Phase 3: Analytics join (Day 6). DONE (API + MCP; UI pending)
- [x] Pull page traffic from Lodd's API (utils/lodd.ts, env-driven, getmasset.com site)
- [x] Join per page: traffic + target keywords + live rank, via GET /api/scoreboard and the MCP tool page_scoreboard. Also surfaces content-gap pages (traffic, no keyword) and unmatched keywords.
- [x] A visible Scoreboard view in the UI (new "Scoreboard" tab on the domain page; UTM variants aggregated, rank 0 shown as "Not ranked", content-gap and no-traffic sections)
- [ ] Minor: aggregate UTM URL variants by clean path (so "/" and "/?utm_medium=redirect" merge); display rank 0 as "not ranked"

### Analytics parity with Lodd: DONE (zero gaps; provider + API + MCP)
- [x] s33k now collects and exposes AT LEAST every Lodd datapoint, and beats it. Full mapping, live sample values, and the conclusion are in `PARITY.md`.
- [x] Provider interface (`utils/analytics.ts`) extended with `getSummary`, `getBreakdown`, `getTimeSeries`, `getEvents`, `getEngagement`; implemented in both `utils/lodd.ts` and `utils/umami.ts`; graceful "not configured" provider covers all of them.
- [x] Lodd parity (verified live, real getmasset.com): totals, per-page, referrers+utm, ai-sources, countries, devices, browsers, operating-systems, session-scores/engagement, events. **Zero GAPs.**
- [x] Extras beyond Lodd (Umami-only): region, city, language, screen, daily time series. On the Lodd provider these correctly report "Not supported by Lodd"; on Umami they return real rows.
- [x] New REST routes (Bearer-key reachable): /api/summary, /api/breakdown, /api/timeseries, /api/events, /api/engagement. New MCP tools: traffic_summary, traffic_breakdown, traffic_timeseries, top_events, engagement.

## Phase 4: AEO = AI referral tracking from analytics (no LLM queries). DONE
- [x] AEO is measured from real analytics REFERRAL data, not by querying any LLM. `getReferralSources` + `classifyReferrer` (utils/ai-sources.ts) detect which AI engines (ChatGPT, Claude, Perplexity, Gemini, Copilot, etc.) actually send visitors.
- [x] Surfaced via GET /api/ai-referrals and the MCP tool `ai_referrals`: per-engine visitors/pageviews plus totals (AI visitors, all referred visitors, AI share of referred traffic). Live: ChatGPT 2, Claude 1, 6% AI share.
- [x] Per-page AI-referral attribution joined into the scoreboard: the scoreboard now attributes AI-referral visitors to their landing pages (with graceful `referralError` / `aiReferralVisitors: 0` degradation if the referral fetch fails, never a 500).

## Phase 6: Auto-discovery onboarding. DONE
The 5-minute-to-value bar applied to setup: a new user should not have to hand-type their page list. The `discover_pages` MCP tool (and `GET /api/discover`) crawls a supplied domain, follows same-origin links, and returns the live page list so the LLM can map keywords to real target pages in one shot.
- [x] `utils/site-crawl.ts`: same-origin BFS crawler that returns the discovered page set. Hardened against SSRF (see below).
- [x] `GET /api/discover?domain=...`: Bearer-key reachable (whitelisted in `utils/verifyUser.ts`), GET-only, validates `domain`, strips query strings.
- [x] MCP tool `discover_pages`: surfaces the crawl to the LLM so onboarding is "point at your domain, I will find your pages" instead of manual entry. Live verified: getmasset.com returns 25 pages.
- [x] **SSRF hardening** (the hosted-product requirement): `isPublicHostname()` rejects non-http(s) schemes plus loopback / RFC1918 / link-local / CGNAT / IPv6-private literals and `localhost`/`.local` names before any network call. This matters because s33k is becoming a multi-tenant hosted product where untrusted tenants hold API keys, and the `169.254.169.254` cloud-metadata endpoint is the classic credential-theft vector. Live verified: getmasset.com returns 25 pages; `localhost`, `127.0.0.1:3005`, and `169.254.169.254` return 0 pages with a graceful error and leak no internal content. Residual: `redirect: 'follow'` could 302 to an internal host; documented in-code as a network-egress-layer concern (closing it fully would risk breaking legitimate apex->www redirects).

## Phase 5: Ship V1 (Day 7). DONE (install package + docs hardened)
- [x] Package so a friend can install: `deploy/docker-compose.yml` (s33k + Umami + Postgres in one stack) and `deploy/.env.template` (placeholders and throwaways only, with openssl regenerate instructions; zero real secrets). README rewritten with the three-pillar story, install steps, and the full 20-tool MCP table.
- [x] **Installable-product docs hardened (V1 install path).** `README.md`, `.env.example`, and `mcp/README.md` made docs-correct so a friend can clone and reach a working install with no surprises:
  - Corrected MCP tool count to **20** (was 19, missing `briefing`); regrouped all 20 tools by pillar (cross-pillar / SEO / AEO / analytics / domains) with one example prompt each, verified against `mcp/src/index.ts`.
  - Added a "What V1 is and is not" honesty section, a from-source Quickstart, a portable JSON MCP config block, and a Hosting pointer to `DEPLOY.md`.
  - Rewrote `.env.example` to cover every env var the app and MCP actually read (grep-verified), each with a comment, a safe placeholder, and required-vs-optional marking. Uses `USER_NAME` (not bare `USER`, which shells export as the OS username).
  - Rewrote `mcp/README.md` (was stale at 5 tools / 7-route whitelist) to the full 20-tool table and the complete route whitelist, cross-checked against `utils/verifyUser.ts`.
  - Install-path verification fixes applied: added `LODD_SITE_DOMAIN` (read by `utils/lodd.ts`, was missing) and `PORT` (read by `cron.js`, was missing) to `.env.example`; aligned the documented default to `:3000` everywhere, including the leftover `:3005` dev default in `mcp/src/index.ts` (now `:3000`, matching the Quickstart and the docker-compose fallback).
- [ ] Open the MCP pull request upstream
- [ ] Decide the model: free lead-gen for Masset, paid, or open-core (log in roadmap)

## Deploy readiness: a documented, secured 10-minute Railway deploy. DONE
The gap was that a fresh container started with an empty DB, no scraper key, and the public SerpBear demo credentials, so a naive deploy was both broken and unsafe. Closed three ways:
- [x] **Production credential guard.** `entrypoint.sh` refuses to boot when `NODE_ENV=production` and `APIKEY`, `SECRET`, or `PASSWORD` are unset, left as a `REGENERATE_ME` placeholder, or set to the public SerpBear demo values. A second guard in `pages/api/login.ts` blocks login under the same conditions (defends the case where `node server.js` is run directly, bypassing the entrypoint). Both guards gate on `NODE_ENV === 'production'`, so local dev with the demo defaults is byte-for-byte unchanged. The guard uses an explicit `exit 1`; `set -e` was deliberately NOT added, so a migration that fails (or is already applied) still boots the app exactly as before.
- [x] **Env-configured SERP scraper.** `getAppSettings` (in `pages/api/settings.ts`, mirrored in `cron.js`) now falls back to `SERPER_API_KEY` (or `SCAPING_API`) for the key and `SCRAPER_TYPE` for the backend when none is set in the Settings UI. A DB-stored value always wins, so existing UI-configured instances are unchanged; a fresh hosted deploy gets a working scraper from env with no UI step. Verified in isolation: DB value wins, env only fills an empty key, `SCRAPER_TYPE` only fills the default `'none'`. Test: `__tests__/pages/settings-serp-env-fallback.test.ts`.
- [x] **`DEPLOY.md`: the copy-pasteable Railway recipe.** Generate strong secrets (openssl), create the service from the Dockerfile, mount a persistent volume at `/app/data` (the one un-skippable step, since the Dockerfile `rm -rf data` means no volume = data wiped every redeploy), set env (auth, public URL, Serper, Umami pointing at the existing Railway instance), a security checklist, and post-boot keyword-seeding curl commands. Zero real secrets in the file (only `REPLACE_*` placeholders). Tests for the guards: `__tests__/pages/login-demo-credentials.test.ts`.

## Daily Briefing: the proactive analyst (20th MCP tool). DONE
The "tell me what to DO, not just what happened" capability, and the recommended FIRST call each day.
- [x] **`GET /api/briefing?domain=...&period=...`** (Bearer-key reachable, whitelisted in `utils/verifyUser.ts`, GET-only). RULES-BASED, no LLM call: it pulls every s33k pillar once (traffic, human-vs-bot, SEO rank + opportunity pages, AI referrals, engagement), runs small transparent commented rules over the joined data, and returns a narration-ready `{ headline, sections[], recommendations[], generatedFor }`. The user's LLM narrates it as a morning standup; the server does the joins and prioritization.
- [x] **Never 500s on a sub-signal failure.** Each pillar is fetched independently and its error is swallowed into a per-section note, so a dead provider or empty table degrades one section, not the whole briefing. The only 4xx paths are auth (401) and missing domain (400). Test: `__tests__/pages/briefing-degradation.test.ts`.
- [x] **MCP tool `briefing`.** Surfaces the briefing to the LLM with a description that tells it to lead with the headline and recommendations. Live verified on real getmasset.com data: correctly reports ~58% bot share (plan against the human figure), the `/resources/claude-artifacts-for-marketers` opportunity page (real traffic, no tracked keyword), Claude + ChatGPT AI referrals, and a 97% bounce caveat, with three prioritized recommendations.

## The 10x: AI-native signals beyond commodity analytics. DONE
The differentiator over Lodd/Plausible is being the AI-native analyst across the joined pillars, plus signals dashboards do not capture. Three shipped, each MCP-controllable and Bearer-reachable over REST:
- [DEPRECATED / HIDDEN, branch `hide-ai-crawler-feature`] **Answer-engine bot detection.** This was a feature that detected which answer-engine bots hit a site. It is fully HIDDEN from users: the user-facing surfaces (the `ai_crawlers` MCP tool, the `GET /api/ai-crawlers` route, and every description and doc that read as a current capability) were removed. The backend plumbing is left DORMANT for a possible future revival: `utils/ai-crawlers.ts` (the classifier), `database/models/crawlerHit.ts` + its migrations, and `pages/api/crawler-hit.ts` (env-gated, now unreachable via the allowlist) remain on disk but are not wired to any user-facing surface. AEO is now measured by AI REFERRALS only (see the AI Visibility section below).
- [x] **Human-vs-bot traffic filtering** (`utils/bot-filter.ts`, `pages/api/human-traffic.ts`, MCP `human_traffic`). Ports the working heuristic from `lodd-traffic/traffic.py`: a segment is bot-suspected when bounce rate is ~99 to 100% AND average duration is very low (under ~15s); engaged sessions (2+ pages or an event) and known-human referrers (search/social/AI) are a human floor. Live on real getmasset.com data it flags the heavy Hong Kong / Singapore / China ~97%-bounce traffic. Reports human-vs-bot split so the marketer trusts the numbers.
- [x] **Cross-pillar insights** (`pages/api/insights.ts`, MCP `insights`). Joins SEO + analytics + AEO into plain-language findings and recommendations (e.g. high-traffic pages with no target keyword, keywords ranking with no traffic, AI-referral signal). Aggregates page rows by clean path first to avoid the UTM "last variant wins" trap.

## AI Visibility: the headline novel feature. DONE (REFERRAL-ONLY since branch `hide-ai-crawler-feature`)
A marketer's standing in AI search, measured by what AI engines actually DO on the site (first-party, un-gameable), never by querying an LLM. NOTE (branch `hide-ai-crawler-feature`): the original design joined two signals (answer-engine CRAWLS as the leading indicator AND AI REFERRALS as the outcome) into a crawl-vs-referral funnel. The crawl half is now HIDDEN; AI Visibility is measured by AI REFERRALS only, with a deterministic AI-citability audit fallback when referral data is thin. The bullets below describe the original funnel design and are kept as historical record.
- [x] **`GET /api/ai-visibility?domain=...&period=...`** (Bearer-key reachable, whitelisted in `utils/allowedApiRoutes.ts`, GET-only). Follows the wired analytics-route pattern: `authorize()` then a `Domain.findOne({ domain, ...scopeWhere(account) })` ownership gate (403 if not owned), exactly like `scoreboard.ts`. Reuses the existing read paths: referrals from `getAnalyticsProvider().getReferralSources()` filtered to `isAI` (same path as `ai-referrals.ts`).
- [x] **The synthesis.** Per page a status of `ai-cited` or `not-cited`. Per engine a status of `advocate` or `absent`. Plus a summary (totalAIReferrals, topAdvocate engine).
- [x] **Never 500s on a sub-signal failure.** The referral read degrades into its own error field; the citability audit is wrapped so it can never break the route. Honest about provider limits: when the provider reports referrals only site-wide (no landing path), per-page `isCited` is not attributed and a `note` flags it, while engine-level referrals and totals stay accurate.
- [x] **Optional enrichment: the AI-citability audit** (`utils/citability-audit.ts`). When first-party data is thin (no referrals), the route fetches the top pages and deterministically scores AI-readiness (llms.txt, Markdown twin, JSON-LD, answer-shaped content), 25 points each. Reuses the SSRF-guarded, timeout-bounded `safeFetchText` from `utils/site-crawl.ts` (now exported alongside `isPublicHostname`); rejects HTML masquerading as a `.md` twin so a soft-404 cannot inflate the score. Live true-positive on getmasset.com: 100/100 (llms.txt + .md twins + JSON-LD on every page).
- [x] **MCP tool `ai_visibility`** (rich self-explaining description). Complements `ai_referrals` (raw referral detail) by adding the per-page view and the citability audit.

## Guided onboarding: domain in, everything set up (22nd + 23rd MCP tools). DONE
The 5-minutes-to-value bar made into one call. The only user input is their domain; the LLM (via MCP) does the rest, across both value moments: the instant no-install path (rankings) and the tracking-code path (analytics + AEO).
- [x] **Per-domain Umami website provisioning + resolution.** New nullable `umami_website_id` column on the Domain model, with an idempotent migration (`1750147200004-...`) matching the `...002`/`...003` `describeTable`-guard style. `utils/umami-provision.ts` `createUmamiWebsite(domain)` POSTs `/api/websites` to the self-hosted Umami instance (auth reused from `utils/umami.ts` `getToken`, no duplication) and never throws (returns `{ websiteId, error }`). `utils/umami.ts` `resolveWebsiteId` now resolves the website id PER DOMAIN: the Domain row's `umami_website_id` first, then the `UMAMI_WEBSITE_ID` env fallback (so getmasset.com is byte-for-byte unchanged), then a `/api/websites` lookup. Flag-safe.
- [x] **Install-guide library** (`utils/install-guides.ts`). `getInstallGuides(domain, websiteId)` returns the exact tracking snippet plus accurate, copy-paste install steps for 8 platforms: raw HTML, Google Tag Manager, WordPress, Webflow, Shopify, Squarespace, Wix, and Next.js/React. Pure product knowledge: no network, no LLM, no secrets.
- [x] **Heuristic keyword discovery** (`utils/keyword-discovery.ts`). `discoverKeywords(domain)` reuses the SSRF-guarded `crawlSite` (no crawl/fetch duplication) and proposes candidate target keywords per page from on-page signals only (title with brand-segment stripping, h1, meta description, h2, slug), filtered/deduped/capped. No server-side LLM. GSC-query discovery is a future layer.
- [x] **The onboard orchestration: `POST /api/onboard` + the `onboard` MCP tool (22nd).** Follows the wired multi-tenant pattern (`authorize` then `scopeWhere`/`ownerIdFor`, owner-stamps the created domain). Given `{ domain }` it: finds-or-creates the (owner-scoped) domain, runs heuristic discovery, adds up to 20 deduped keywords each mapped to its page as `target_page` and queues the background SERP scrape (reuses `refreshAndUpdateKeywords`, fire-and-forget, exactly like `addKeywords`), provisions a per-domain Umami website and stamps `umami_website_id`, and returns `{ domain, discoveredKeywords, addedKeywords, rankingsPending, umamiWebsiteId, installSnippet, installGuides, note }`. Degrades gracefully: an Umami provisioning failure returns `umamiWebsiteId: null` + a note instead of 500-ing; domain, keywords, and rankings still come back.
- [x] **On-demand install tool: `GET /api/install-instructions` + the `install_instructions` MCP tool (23rd).** Returns the snippet + guides for an already-onboarded domain (ownership-gated, 403 if not owned), resolving the id from `Domain.umami_website_id` then the `UMAMI_WEBSITE_ID` env fallback. Both new routes whitelisted in `utils/allowedApiRoutes.ts`.
- [x] **MCP now exposes 23 tools.** `tools/list` over stdio returns 23 with `onboard` and `install_instructions` present; the stderr banner reads "23 tools registered."

## Test suite. DONE (full suite green)
- [x] Jest suites for the new utils: `__tests__/utils/ai-crawlers.test.ts`, `bot-filter.test.ts`, `bot-filter-aggregation.test.ts`, `ai-sources.test.ts`, `lodd.test.ts`, `site-crawl.test.ts` (10/10, includes a 6-target SSRF test) plus `site-crawl-edge.test.ts` (entity decoding, og:description fallback, script/style stripping, non-content pruning, depth ordering, link de-dup, more SSRF host classes), `lodd-domain-scope.test.ts`, `scope.test.ts`, `resolveAccount.test.ts`, the scoreboard AI-attribution path `__tests__/pages/scoreboard-ai-attribution.test.ts` (landing-path attribution, the no-landing-path note, the never-500 referral-throw degrade, and clean-path matching), and the deploy-readiness + briefing suites: `__tests__/pages/briefing-degradation.test.ts` (every pillar independently caught, never 500s, 400 missing domain), `__tests__/pages/login-demo-credentials.test.ts` (prod blocks demo/placeholder creds; dev unchanged), `__tests__/pages/settings-serp-env-fallback.test.ts` (DB value wins; env only fills empty). Plus the AI Visibility Funnel suites: `__tests__/pages/ai-visibility.test.ts` (funnel math, the four page statuses, the three engine statuses, topAdvocate / biggestGap, the owner-to-engine join, graceful degradation on each sub-signal, the site-wide-referrals note, and the thin-data citability trigger) and `__tests__/utils/citability-audit.test.ts` (a getmasset.com-shaped 100/100 true-positive, per-signal scoring, HTML-masquerading-as-md rejection, unfetchable-page degradation, root inclusion, de-dup, and the 8-page cap).
- [x] Guided-onboarding suites (4 suites / 27 tests): `__tests__/utils/keyword-discovery.test.ts` (heuristic extraction on fetch-mocked HTML, brand-segment stripping, trust ordering, stop-word/number filtering, dedupe, graceful empty on unreachable sites), `__tests__/utils/install-guides.test.ts` (snippet/script-URL resolution, all 8 platforms, accurate GTM/Shopify/Next.js steps), `__tests__/utils/umami-website-resolution.test.ts` (`resolveWebsiteId` per-domain-first then env fallback then lookup, no-match error), and `__tests__/pages/onboard.test.ts` (owner-stamped find-or-create, domain normalization, global keyword dedupe + `target_page` mapping + 20-cap, fire-and-forget refresh, per-domain Umami provision + stamp, tenant vs admin stamping with real `ownerIdFor`/`scopeWhere`, graceful provision-failure degradation, and 401/405/400 guards).
- [x] **Full suite green: 34 suites pass, 246 tests pass.** (Was 24/158 at the deploy-readiness pass, then 28/194 before the AI Visibility feature, then 30/219; the four guided-onboarding suites add 4 suites / 27 tests.) The 2 previously-failing UI/hook suites (`__tests__/hooks/domains.test.tsx`, `__tests__/pages/domain.test.tsx`) are fixed. Fixes were confined to the test harness and test files (no product code changed):
  - `jest.setup.js`: polyfilled the web globals MSW 2.x references at import time but jsdom on Node 20 lacks (`TextEncoder`/`TextDecoder` from `util`, `BroadcastChannel` from `worker_threads`, `WritableStream`/`ReadableStream`/`TransformStream` from `stream/web`).
  - `jest.config.js`: awaited the resolved next/jest config and **replaced** `transformIgnorePatterns` with a negative-lookahead allowlist (`msw`, `@mswjs`, `until-async`, `@bundled-es-modules`, `headers-polyfill`, `outvariant`, `strict-event-emitter`, `is-node-process`, `graphql`) so Jest transpiles the MSW ESM dependency chain instead of skipping it.
  - `__tests__/pages/domain.test.tsx`: supplied the `useUpdateSettings` mock implementation (`{ mutate, isLoading }`) that `KeywordsTable.tsx` destructures, which the auto-mock left `undefined`. Also corrected the "Country Filter" test: it is byte-identical to upstream SerpBear and asserted `toBe(0)` on a single-country (US-only) dummy dataset where selecting the only dropdown option ("United States") cannot drop the list to zero; rescoped to assert the single option is "United States" and that selecting it keeps both US keywords (`toBe(2)`), with an inline comment documenting the upstream bug so it is not silently masked.

## Analytics ownership: replace Lodd with self-hosted Umami (the standalone/sellable requirement)
- [x] Integration code: analytics layer is now provider-pluggable (utils/analytics.ts). Umami provider written (utils/umami.ts); Lodd demoted to legacy/dev. Default provider is umami. Lodd no longer load-bearing.
- [x] Host the instance: self-hosted Umami now runs publicly on Railway (Umami + Postgres). This is the owned analytics stack.
- [ ] Point s33k at the live Umami in production and add the Umami tracking script to getmasset.com so owned data starts flowing. (s33k/.env already points UMAMI_* at the Railway instance; ANALYTICS_PROVIDER stays 'lodd' for live verification until the tracking script is live, a separate human-approved step.)

### Closed gaps (this pass)
- [x] **Lodd per-domain scoping.** The Lodd provider read a fixed `LODD_SITE` regardless of the requested domain, so per-domain calls returned getmasset.com data for every domain. Added a domain-scoping guard: a request for a domain that does not match the configured Lodd site now returns zeros plus an explanatory `loddError`, instead of silently mislabeling getmasset.com data as another domain's. Test: `__tests__/utils/lodd-domain-scope.test.ts`. Live verified: `competitor.com` returns zeros + error.
- [x] **Umami per-page grain.** The Umami provider exposed aggregate counts but not per-page bounce_rate / avg_duration / parsed-UTM at that grain; extended so per-page rows carry the finer metrics the scoreboard and bot-filter need.
- [ ] `get_insight` still needs Google Search Console connected (Phase 1.5). Out of scope for this pass.

## Multi-tenant foundation (non-breaking). DONE (schema + scope helpers, shipped dark)
The standalone hosted-product step. Shipped as a non-breaking foundation behind a `MULTI_TENANT` flag (unset by default), so single-admin deployments are byte-for-byte unchanged. Full design in `MULTI_TENANT.md`.
- [x] Schema: new `account` and `api_key` tables (models + migrations), plus nullable `owner_id` on `domain` and `keyword`. All four migrations idempotent (the repo's `resolveQueryInterface` + `describeTable` dual-convention). `account` seeds exactly one row (the existing admin); all existing domain/keyword rows get `owner_id: null`, untouched.
- [x] Scope helpers: `utils/scope.ts` (`scopeWhere` / `ownerIdFor`) and `utils/resolveAccount.ts` short-circuit to today's behavior when `MULTI_TENANT` is off. Imported only by their own tests and the two model registrations; no production route adopts them yet.
- [x] Verified non-breaking live: every existing endpoint returns 200 with unchanged data (the only row delta is the additive `owner_id: null`).
- [ ] Next (the real hosted-product step, with Ben present): take the foundation from non-breaking schema to actually scoped (wire `scopeWhere` into the routes behind the flag, per-tenant API keys).

## Upstream PR plan. DONE (plan documented)
Full plan in `UPSTREAM_PR.md`: which changes are clean upstream contributions to `towfiqi/serpbear` (the MCP server, the one-click GSC OAuth, target-page mapping) versus which stay s33k-specific (analytics provider layer, AEO, multi-tenant). Sequencing and PR boundaries documented.

## Invite-only access: Gmail-style invite system (24th-27th MCP tools). DONE
The access model for the hosted product, shipped behind the same `MULTI_TENANT` flag (additive, flag-safe, single-admin deployments byte-for-byte unchanged). An ACCOUNT is a domain-owning org with two roles: ADMIN (created the account) and MEMBER (read-only, invited by the admin). Billing is per-seat but DEFERRED (no payments built). Full security write-up below.
- [x] **Schema (additive, idempotent).** `invite` table (`code` unique-indexed, `inviter_account_id`, `type` external/internal, nullable `email`, `target_account_id`, `status` pending/accepted/expired/revoked, `accepted_at`, `accepted_by_account_id`) and `waitlist` table (`email` unique-indexed for DB-level dedupe, `domain`, `note`, `status`). Plus additive nullable columns: `account.external_invite_quota` (default 5) and `api_key.role` (default 'admin'). Migrations `...005` through `...008`, same `describeTable`-guard dual-convention style; no backfill, legacy keys keep full admin access.
- [x] **Two invite types.** EXTERNAL brings a brand-new admin + account into s33k (the viral lever), LIMITED per inviter by `external_invite_quota`, enforced by COUNTING the inviter's pending+accepted external invites (no racy counter mutation). INTERNAL adds an unlimited read-only MEMBER seat to the caller's OWN account.
- [x] **Endpoints.** `POST /api/invite` (authed admin; member keys 403'd) creates an invite and best-effort emails it. `GET /api/invite` (authed admin) lists the caller's sent invites. `POST /api/invite/accept` (PUBLIC, the code IS the credential) validates a single-use, unexpired code and mints either a new admin account+key (external) or a read-only member key on the target account (internal). `POST /api/waitlist` (PUBLIC) adds an email, deduped, no existence leak. `GET /api/waitlist` (root admin only) lists it. The two authed routes are whitelisted in `utils/allowedApiRoutes.ts`; the two public routes deliberately bypass `authorize` (they run pre-account and validate their own input, like the OAuth-callback pattern).
- [x] **Member read-only enforcement (`utils/authorize.ts` + `utils/resolveAccount.ts`).** A per-account key's `role` is surfaced on resolution; `authorize()` rejects any non-GET request from a `role: 'member'` key with 403 `Read-only member` before it reaches the route. Admin and legacy keys are unaffected. Flag-safe: member keys only exist with `MULTI_TENANT` on.
- [x] **Accept-endpoint security.** Codes are ~200-bit cryptographically-random base62, single-use, and expirable (30-day TTL derived from `createdAt`, no new column). Every bad-code path (unknown, non-pending, expired, revoked, malformed, missing/inactive internal target) returns ONE byte-identical generic rejection (`Invalid or expired invite code.`), so the endpoint cannot enumerate codes, accounts, or emails. A lightweight per-IP in-memory rate limiter (10/60s) blunts brute-force probing on top of the code entropy. **Single-use is now race-safe:** acceptance CLAIMS the invite with an atomic conditional update guarded by `status: 'pending'` (only one of N concurrent replays flips the row; the losers see 0 affected rows and reject generically, minting nothing) BEFORE any account/key is created, then stamps the consuming account. This closed a TOCTOU race the original read-status-then-update left open on the key-minting path.
- [x] **Web pages (thin, SSR-safe).** `pages/invite/[code].tsx` (accept page: shows the invite, a name field, POSTs to the public accept route, then renders the minted API key once + the MCP config block + an onboarding hint, branching copy on admin-vs-member). `pages/waitlist.tsx` (email + domain, POSTs to the public waitlist route, same thank-you whether or not the email is new). Minimal styling, not a polished design pass.
- [x] **Invite email (`utils/send-invite.ts`).** Sends via the Resend HTTP API using `RESEND_API_KEY`. Graceful and side-effect-only: if the key is unset (current state) or the send fails, it logs and returns `{ sent: false }`, never throws, never blocks the invite; the caller falls back to returning the link. On-brand HTML, `RESEND_FROM_EMAIL` overridable.
- [x] **MCP now exposes 27 tools.** Added `invite_external`, `invite_internal`, `list_invites`, `list_waitlist` (each rich, self-explaining, admin-key-required). `tools/list` over stdio returns 27 with all four present; the stderr banner reads "27 tools registered."

## Invite-system test suite. DONE (full suite green)
- [x] **Full suite green: 37 suites pass, 276 tests pass** (was 34/246 before the invite system; +3 suites, +30 tests). Three new suites: `__tests__/pages/invite-routes.test.ts` (external quota allow/deny + never-create-on-deny, unlimited internal invites, member-key 403 on both POST and GET, external accept mints admin account+key + race-safe single-use claim/stamp, internal accept mints a member key, and the generic rejection of unknown/used/expired/empty/concurrently-raced codes with nothing minted), `__tests__/pages/waitlist-routes.test.ts` (public create + dedupe + concurrent unique-constraint swallow + email validation + admin-only GET), and `__tests__/utils/authorize-member-readonly.test.ts` (member allowed on GET, rejected on POST/PUT/DELETE; admin and legacy keys unaffected). All pure (no network, no DB): models, `authorize`, and `send-invite` mocked.

## Autocapture engagement analytics: the GA4-killer (28th-31st MCP tools). DONE
GA4 makes you instrument every button and form in Tag Manager. s33k flips it: ONE script tag autocaptures rich engagement with ZERO per-element setup, and the read side is MCP-queryable (no dashboard to configure either). The non-negotiable promise: capture the EVENT, never the PII. Cookieless, no fingerprinting, so the rich-analytics feature and the zero-fear privacy stance stay aligned.
- [x] **The autocapture client (`public/s33k.js`).** Plain, dependency-free browser JS served by s33k. One `<script defer src=".../s33k.js" data-domain="example.com">` tag autocaptures five event types: clicks on buttons/links (visible text + a short stable CSS selector + page), form submissions (form id/name + page), active engagement seconds (paused on visibilitychange/blur/idle), max scroll depth percent, and outbound link clicks (destination host only). Batches and POSTs to the first-party `/api/collect`. SAFE: every handler is in try/catch under an outer catch so it can never throw into the host page; passive listeners; debounced scroll; `keepalive` fetch + `sendBeacon` on the way out. Honors Do-Not-Track (bails entirely). Cookieless anonymous session = a daily-rotating djb2 hash of UA + date + a per-tab random seed, in `sessionStorage` only (in-memory fallback), not joinable across days or tabs, not identifying.
- [x] **PRIVACY: no-PII enforced in BOTH the script and the ingest.** Client side: `elementText()` reads `textContent`/`aria-label`/`title`/`img[alt]` only and has an explicit guard that NEVER reads `.value` for input/textarea/select; there is no code path that reads keystrokes, `.value`, `FormData`, `innerText`, or `[contenteditable]` text (verified by grep: zero hits). Forms record id/name only, never field values; outbound records the host only, never the full URL/query. Server side (`utils/event-sanitize.ts`), defense-in-depth: every event is sanitized BEFORE any DB write; label/selector are control-char-stripped, whitespace-collapsed, truncated, and any label/selector matching an email / credit-card / SSN / phone pattern DROPS the whole event; `value` is kept only for scroll (0-100) and engagement (0-86400s); page is reduced to a pathname with query+hash stripped. A tampered client that smuggles a typed value cannot persist it.
- [x] **The PUBLIC ingest (`POST /api/collect`, no API key).** It takes no key because a script in a stranger's browser cannot hold a secret, so it is deliberately NOT in `allowedApiRoutes.ts`. It defends itself, in order, never throwing: CORS preflight 204 / non-POST 405; missing domain 400; bot-UA dropped via `isLikelyBotUA` (reuses `classifyCrawler` + generic bot hints) so the domain is never even looked up; sanitize+PII-strip the batch (cap 50/req); per-(ip+domain) sliding-window rate limit (600/60s, over-limit returns 200/recorded:0 to avoid retry-storms); domain allow-list `Domain.findOne({ domain })` -> 403 unknown (stores nothing); tenant stamping (`owner_id` copied from the owning Domain onto every row); skip-and-continue per-row `create` so one bad row never 500s the batch. A genuinely broken request returns a clean 400, never a stack trace.
- [x] **The store (`s33k_event` model + idempotent migration `...009`).** `{ ID, domain, owner_id, type, page, label, selector, value, session, created }`, indexed on domain/owner_id/created, same `describeTable`-guarded dual-convention idempotent migration style as the rest of the repo; `down` drops the table. Records THAT an interaction happened, never what was typed.
- [x] **Read surfaces (4 MCP tools + REST routes, all ownership-gated).** `top_clicks`, `form_submissions`, `scroll_depth`, `page_engagement` over `GET /api/top-clicks|form-submissions|scroll-depth|page-engagement` (all whitelisted for Bearer callers; `/api/collect` stays public/un-whitelisted). Each follows the wired analytics pattern: `authorize()` -> GET-only -> domain required -> `Domain.findOne({ domain, ...scopeWhere(account) })` 403 if not owned, and the `S33kEvent.findAll` read itself ALSO carries `...scopeWhere(account)` so a domain-name collision cannot leak another tenant's events. Aggregation logic lives in pure `utils/eventReports.ts` (clicks count + per-page breakdown; form counts + total; scroll avg/max + 0-25/25-50/50-75/75-100 histogram; engagement avg/total/site-avg), period-filtered via `eventPeriodCutoff` (the same `[dhwm]` grammar the crawler/analytics windows use). Never queries an LLM.
- [x] **MCP now exposes 31 tools.** Added `top_clicks`, `form_submissions`, `scroll_depth`, `page_engagement` (each rich, self-explaining, privacy-noting). `tools/list` over stdio returns 31 with all four present; the stderr banner reads "31 tools registered."
- [x] **Test suite (6 new suites / +69 tests, full suite green at 43 suites / 345 tests).** `__tests__/utils/event-sanitize.test.ts` (email/card/SSN/phone drop, unknown-type reject, scroll/engagement clamp, page query+hash strip, batch cap), `__tests__/utils/collect-guards.test.ts` (bot-UA detection, client-IP, rate-limit cap/reset/scope), `__tests__/pages/collect-route.test.ts` (unknown-domain 403 stores nothing, bot drop, owner_id stamping, PII event dropped while clean sibling kept, skip-and-continue, missing domain 400, GET 405), `__tests__/pages/collect-route-resilience.test.ts` (malformed body never 500s, rate-limit boundary, throwing Domain.findOne -> clean 400, OPTIONS 204), `__tests__/utils/event-reports.test.ts` (all four aggregations + period cutoff), `__tests__/pages/event-routes-ownership.test.ts` (adversarial cross-tenant `describe.each` over all 4 routes: 403 + zero event reads on unowned domain; owned domain scopes BOTH the ownership lookup and the event read by owner_id; admin unscoped). The original 276 untouched.

## Conversions by source: which traffic sources actually convert (40th MCP tool). DONE (full suite green)
GA4 makes conversion attribution painful: configure conversions, set up channel groups, then wait. s33k flips it. The autocapture client already records form submissions (`form_submit`); this adds first-touch SOURCE attribution so the question "which traffic sources actually drive my conversions" is answered with one script tag and one MCP query, no GA4 setup.
- [x] **First-touch source on every event (client + store).** `public/s33k.js` reads `document.referrer` ONCE at session start and classifies it to `direct` | `organic-search` | `ai` | `referral` (a small, self-contained matcher that mirrors the server's `utils/ai-sources.ts` notions but cannot import server code). It carries ONE top-level `source` on every batch. PRIVACY (non-negotiable, same guarantee as the rest of autocapture): the classifier reads host + path only (query and hash explicitly excluded), and the most identifying value it can ever emit is a bare referral HOST, never a full referrer URL with query params. A nullable `source` column was added to the `s33k_event` model with an idempotent, `describeTable`-guarded, dual-convention migration (`...011`) indexed for cheap grouping; existing rows get NULL (no backfill, the read treats missing as `direct`).
- [x] **Ingest privacy gate (`utils/event-sanitize.ts` + `/api/collect`).** New `sanitizeSource()` is the storage gate: it returns ONLY one of the four classes or a STRICT bare host (`/^[a-z0-9.-]+(?::\d{1,5})?$/` with a `.tld` tail, then PII-checked), and DOWNGRADES anything URL-like (any `/`, `?`, `#`, `://`, `@`, or whitespace) to `direct`. So a tampered client that posts a full referrer URL with `?email=...` can never write an identifying source. `sanitizeBatch` stamps the one sanitized session source on every event; `/api/collect` passes `body.source` through and defaults absent source to `direct`. Stays a never-500 skip-and-continue loop; `/api/collect` remains public and un-whitelisted.
- [x] **Read surface (`GET /api/conversions`, MCP `conversions_by_source`, 40th tool).** Ownership-gated exactly like the other event routes: `authorize()` -> GET-only -> domain required -> `Domain.findOne({ domain, ...scopeWhere(account) })` 403 if not owned, and the `S33kEvent.findAll` read ALSO carries `...scopeWhere(account)`. `event` defaults to `form_submit`; any captured type can be chosen. Pure aggregation in `utils/eventReports.ts` (`buildConversionsBySource`): per-source counts + share of total, the top converting source, and an HONESTLY-APPROXIMATE conversion rate (conversions / distinct sessions that fired any event under that source in the window, with a `conversionRateNote` explaining the denominator and that the true rate is no higher). Never queries an LLM; never 500s on a thin sub-signal. Whitelisted `GET:/api/conversions`; required `CapabilityEntry` added to `utils/knowledge.ts` so the knowledge-coverage test passes.
- [x] **MCP now exposes 40 tools.** Added `conversions_by_source` (rich, self-explaining, privacy-noting, with the honest-approximate-rate caveat in its description). Live `tools/list` over stdio returns 40 with it present; the stderr banner reads "40 tools and 5 resources registered."
- [x] **Test suite (+ source coverage, full suite green at 54 suites / 533 tests).** Extended `event-sanitize.test.ts` (source classes pass, bare host passes, URL/path/query/`@`/PII downgraded to `direct`), `event-reports.test.ts` (`buildConversionsBySource` counts/share/top/legacy-null->direct/approx-rate+note/arbitrary event type/empty non-throwing), `collect-route.test.ts` (URL source stored as `direct`, class stamped on every row, missing source -> `direct`); added `__tests__/public/s33k-source-classification.test.ts` (the REAL client in JSDOM: the four classes, deep referral URL leaves only the bare host, PII search URL collapses to `organic-search`, malformed referrer -> `direct` without throwing) and `__tests__/pages/conversions-route.test.ts` (unowned-domain 403 with zero event reads, owned-domain owner_id scoping, admin unscoped, group-by + approx-rate note, `?event=` override, empty -> 200 zeros, missing domain 400, non-GET 405).

## Trust and data ownership: start a trial with zero security fear (32nd-34th MCP tools). DONE (full suite green)
The strategy is real guardrails plus VERIFIABLE assurances, leaning on the strongest assets: open source + self-hostable ("verify us, don't trust us"), no-training that is STRUCTURALLY true, real tenant isolation, cookieless/no-PII tracking, and secrets encrypted at rest.
- [x] **Data export (`GET /api/export`, MCP `export_data`).** `authorize()` + `scopeWhere(account)` returns ONE JSON bundle of ALL the caller's data: domains, keywords (with full rank history), crawler hits, autocapture events, and account + API-key metadata. Tenant-scoped so it only ever returns the caller's own rows. NEVER emits a secret: the encrypted `search_console` blob is stripped to a boolean `search_console_configured`; API keys come back as non-sensitive metadata only (prefix, name, role, timestamps), never `key_hash` and never the clear key (which is mint-once and unrecoverable). Whitelisted `GET:/api/export`.
- [x] **Hard delete (`DELETE /api/account-data`, MCP `delete_account_data`).** The ultimate "your data is yours." Permanently and irreversibly deletes the caller's ENTIRE account: every domain, keyword, crawler hit, autocapture event, the account's api_keys, the account row, plus a best-effort deprovision of each per-domain Umami website (`deleteUmamiWebsite`, never throws; failures collected as warnings, deletion proceeds). THREE guardrails enforced server-side: (1) CONFIRMATION-GATED, body must be exactly `{ confirm: "DELETE" }` or 400, nothing deleted; (2) ADMIN REFUSED, the admin/legacy account (ID === ADMIN_ACCOUNT_ID, including the null-account single-tenant path) is 403, so the route is inert in single-tenant mode; (3) STRICTLY TENANT-SCOPED, every destroy carries `scopeWhere(account)` (owner_id) and/or the caller's own owned-domain set, api_keys/account keyed to the exact id, no wildcard destroy anywhere. The global `@Unique` domain column means a domain name belongs to exactly one account, so domain-name scoping cannot reach another tenant's crawler hits or events. Logs one clear `[DELETE] ... IRREVERSIBLE` line with counts. Whitelisted `DELETE:/api/account-data`.
- [x] **In-code no-training + encryption markers.** Prominent module-level trust markers at the AI-touching routes (`briefing.ts`, `insights.ts`, `ai-visibility.ts`) stating s33k never sends customer data to a model trainer and makes no LLM call (the routes are rules-based; interpretation happens in the USER's own LLM over MCP). Verified structurally true: no LLM client, no model-provider SDK in either package.json, no model-provider host called anywhere, no embedding/fine-tune/train path. Encryption-at-rest markers at `utils/searchConsole.ts` and `utils/adwords.ts`; cryptr (AES-256, keyed by app SECRET) confirmed real in `domains.ts`, `settings.ts`, `searchConsole.ts`, `adwords.ts` for SC keys, Google Ads keys, scraper key, smtp password. API keys stored as SHA-256 hash, clear key shown once.
- [x] **Trust doc + security MCP tool (`GET /api/security`, MCP `security_facts`).** `SECURITY.md` is the honest, source-cited trust doc with a proof index; `utils/securityFacts.ts` is the single dependency-free structured source it mirrors; `GET /api/security` returns those facts (no tenant data, no secrets, same for every caller); the `security_facts` MCP tool wraps it so a trial user asks their own LLM "is this safe? do you train on my data? who else can see it?" and gets a complete, verifiable answer. Covers all six pillars (no-training, tenant isolation, encryption at rest, data ownership, open-source/self-hostable, cookieless/no-PII) plus sub-processors (Railway hosting + Postgres, self-hosted Umami, Serper for SERP). Whitelisted `GET:/api/security`.
- [x] **MCP now exposes 34 tools.** Added `export_data`, `delete_account_data`, `security_facts`. Live `tools/list` over stdio returns 34 with all three present; the stderr banner reads "34 tools registered."
- [x] **Test suite (3 new suites / +28 tests, full suite green at 46 suites / 373 tests).** `__tests__/pages/export-route.test.ts` (8: export returns ONLY the caller's tenant data; never emits a secret/hash; admin path unscoped; empty-domain-set; 401/405), `__tests__/pages/account-data-route.test.ts` (14: refuses without exact `{ confirm: "DELETE" }`; refuses admin including the null-account path; deletes only the caller's own rows; best-effort Umami delete never aborts; 401/405), `__tests__/pages/security-route.test.ts` (6: facts cover all six pillars with proof refs; route returns full facts identical for any caller, leaking no tenant data; 401/405). The prior 345 untouched and still green.

## Self-supporting product: the knowledge layer, help, MCP resources, and feature requests (35th-37th MCP tools). DONE (full suite green)
Two linked goals: (A) s33k answers ANY user question through the user's own LLM so no human support is ever needed, and (B) users can request features that genuinely do NOT exist yet, gated so the LLM can never offer or file a request for something already built. Both ride one authoritative product-knowledge layer (a single source of truth), and a coverage test fails the build if any tool lacks an entry, so the answers can never silently rot.
- [x] **The single source of truth (`utils/knowledge.ts`).** One structured module covering: every MCP tool (one `CapabilityEntry` each with id/toolName/category/title/description/whenToUse/examplePrompt); setup (which REFERENCES `getInstallGuides()`, does not duplicate it); honest design reasoning (6 entries pulled from BUILD_PLAN.md + SECURITY.md: MCP-first, Serper, cookieless Umami, no-model-training, caps/invite model, open source); troubleshooting (6 common issues: rankings pending, empty AI funnel because measurement is new, analytics needs the script, read-only member key, route not whitelisted, Search Console not connected); trust (EMBEDS the live `securityFacts` object by import, single source); and pricing/limits/privacy at a high level. `searchKnowledge(q, topic)` is the pure, never-throws lookup behind help; `crossCheckCapability(request)` is the pure cross-check behind the feature-request safety net. No content is duplicated; install-guides and securityFacts each live in exactly one place.
- [x] **Self-support exposed three ways.** (1) The `help` MCP tool over `GET /api/help?topic=&q=` (authed like `/api/security`, reads no tenant data) is "ask s33k anything," with a rich description telling the LLM to call it for ANY question and to confirm a capability exists before saying it does not. (2) Five MCP RESOURCES (`knowledge://capabilities|setup|reasoning|troubleshooting|trust`), registered with `server.registerResource`, listable via resources/list and readable via resources/read, each pulling its topic-scoped slice from the same `/api/help` source so a client can discover and pull whole docs into context. (3) Self-explaining tool descriptions: every one of the 37 tool descriptions is multi-sentence and self-explaining (audited; the existing 34 needed no rewrite). Whitelisted `GET:/api/help`.
- [x] **Feature requests, gated against existing capabilities.** `request_feature` MCP tool, description HARD-REQUIRES the LLM to confirm via `help` that the capability does NOT exist first and to NEVER offer it for something already built. SERVER-SIDE SAFETY NET: `POST /api/feature-request` runs `crossCheckCapability` (the single "does it exist?" source, shared with help and the coverage test) before any write; a strong match returns `{ stored:false, matched:true, capability, message }` and stores NOTHING, so a duplicate of an existing feature never lands even if the LLM skipped its own check. The match is conservative on both ends (>=2 overlapping significant terms AND a clear lead over the runner-up; meta tools excluded) because a false "already exists" wrongly blocks a real request. A genuinely-new request is stored (`feature_request` model + idempotent `describeTable`-guarded migration `...010`; status open/reviewed/planned/declined/shipped; owner stamped) and the team is best-effort emailed via Resend (`utils/notify-feature-request.ts`, never throws, no-ops when unconfigured). `list_feature_requests` MCP tool over `GET /api/feature-request` is ROOT-ADMIN only (mirrors the waitlist gate; non-admin 403); member keys are 403'd on the POST by `authorize()`. Both authed routes whitelisted.
- [x] **The durability guarantee (coverage test).** `__tests__/utils/knowledge-coverage.test.ts` parses every `registerTool('<name>', ...)` out of `mcp/src/index.ts` (the real registry, parsed from source because the module connects a stdio transport at import) and asserts each has a matching `CapabilityEntry`. A vacuity guard (>=30 tools, known names present, unique), an aggregate "FAILS THE BUILD" assertion that names offenders, a per-tool `it.each` over all 37, an orphan check the other direction, and a meta-test that runs the exact assertion against an undocumented name and asserts it throws. Proven real by temporarily injecting a phantom undocumented tool (2 tests failed, reverted immediately). Adding a tool without a knowledge entry breaks CI.
- [x] **MCP now exposes 37 tools and 5 resources.** Added `help`, `request_feature`, `list_feature_requests`. Live `tools/list` over stdio returns 37 with all three present; `resources/list` returns the 5 knowledge resources; the stderr banner reads "37 tools and 5 resources registered."
- [x] **Test suite (2 new suites / +61 tests, full suite green at 48 suites / 434 tests).** `__tests__/pages/feature-request-routes.test.ts` (safety-net refusal stores nothing; new request stored with owner stamped; graceful notify-failure still 201; empty request 400; member 401; admin-only GET 403 for non-admin; unknown status filter lists all) and `__tests__/utils/knowledge-coverage.test.ts` (the coverage guarantee above, plus help topic-scoping, the cross-check safety net both directions, and admin-only docs in lockstep with the gate). The prior 373 untouched and still green.

## Entry-page (landing-page) analysis: the acquisition-surface cross-pillar join (38th MCP tool). DONE (full suite green)
Entry pages, where a session STARTS, are the acquisition surface and behave differently from deeper pages. This feature segments a domain's entry pages by first-touch source class and JOINS each one to its existing per-page SEO keywords + live rank AND its AI referrals, then synthesizes a status. It is the join nobody else offers: it connects "we rank for X" to "X actually LANDS people." A SEGMENTATION + JOIN over data s33k already has, NOT new collection, and honest where Umami's data is still thin.
- [x] **Umami provider entry-pages read (`getEntryPages(domain, period)` in `utils/umami.ts`).** Two metrics calls: `type=entry` for the exact per-page entry counts (a failure here is fatal: nothing to attribute) and `type=referrer` for the site-wide source mix (a failure here is NON-fatal: pages still return, sources default to all-direct, the referrer error surfaces). Reuses `periodToRange`, `resolveUmami` (so per-domain `umami_website_id` resolution is preserved and getmasset.com is unchanged), and the shared classifier. Two pure exported helpers: `bucketReferrerRows` (referrer rows into the four classes via `classifySourceClass`) and `approximatePageSources` (a page's entry count scaled by the site-wide proportions, rounded so the four buckets sum to `entries` exactly, largest bucket absorbing the remainder, zero-signal falling to all-direct). Added `classifySourceClass` + `SourceClass` to `utils/ai-sources.ts` next to the AI classifier (reuses `classifyReferrer` for the AI case). New `EntryPage`/`EntryPageSources`/`EntryPagesResult` types on `utils/analytics.ts`, added to the `AnalyticsProvider` interface, the `unconfiguredProvider`, and `LoddProvider` (which honestly reports entry-page analysis as unsupported and points at Umami, rather than approximating from the wrong shape).
- [x] **`GET /api/entry-pages` + the `entry_pages` MCP tool (38th).** Ownership-gated exactly like `scoreboard.ts`: `authorize()` then `Domain.findOne({ where: { domain, ...scopeWhere(account) } })` returns 403 before any read (401 unauthorized, 400 missing domain, 405 non-GET). For each entry page it returns `{ page, pathClean, entries, sources, sourcesApproximated, keywords:[{keyword,rank}], aiReferrals, status }` by joining the provider's entry pages with the tracked keywords (matched by `target_page` -> `cleanPath`, the scoreboard's join, via `parseKeywords`) and the AI referrals (the scoreboard's `getReferralSources` landing-path attribution: AI sources only, keyed by `cleanPath(landing_path)`). Plus a top-level `summary` (topLandingPages, biggestRankingNotLandingGap, aiLandingPages, statusCounts) and a `statusLegend`. Never 500s on a sub-signal failure: a keyword-read failure or a referral throw each degrade one signal and the route still returns 200. Whitelisted `GET:/api/entry-pages`. The tool description self-documents all five statuses, the cross-pillar framing, and the approximation honesty.
- [x] **The status classification is a pure, tested function (`utils/entry-page-status.ts`).** `classifyEntryPage({ hasTrackedKeywords, hasNonDirectTraffic, hasAiTraffic })` returns one of five statuses with first-match precedence: `ai-landing` (checked first, highest-signal), `working` (ranks AND lands from search/referral), `ranking-not-landing` (ranks but no non-direct entry traffic, the clearest gap), `brand-direct` (non-direct entries but no tracked rank), `opportunity` (entry traffic but neither). `ENTRY_PAGE_STATUS_LABELS` gives a human one-liner per status, surfaced in the API and MCP.
- [x] **Honest where the data is thin (mirrors ai_visibility).** Umami reports referrers SITE-WIDE, not per landing page, so per-page source splits are APPROXIMATED from the site-wide mix and every page is flagged `sourcesApproximated:true` with `sourcesNote` explaining it; per-page `aiReferrals` is 0 unless the provider exposes a `landing_path`, flagged by `aiReferralNote`. The per-page entry counts and the tracked ranks ARE exact.
- [x] **Knowledge entry + coverage.** Added the `entry_pages` `CapabilityEntry` to `utils/knowledge.ts` (the knowledge-coverage / WF7 test fails the build otherwise). The coverage test is green with the new tool.
- [x] **MCP now exposes 38 tools.** Live `tools/list` over stdio returns 38 with `entry_pages` present.
- [x] **Test suite (2 new suites / +23 tests, full suite green at 50 suites / 457 tests).** `__tests__/utils/entry-page-status.test.ts` (9: all five statuses, the ai-landing precedence rule, an exhaustive all-8-boolean-combinations check, truthy/undefined coercion, the labels) and `__tests__/pages/entry-pages.test.ts` (14: the ownership gate 403/401/400, the keyword join by target_page, per-page AI attribution by landing_path, the ai-landing fallback from the approximated AI share, clean-path matching, the honesty notes, graceful degradation when the referral fetch throws and when the keyword read fails, empty data, and the summary). The prior 434 untouched and still green.

## The proactive analyst: period-over-period "what changed and what to do" (39th MCP tool). DONE (full suite green)
s33k was a passive dashboard. This makes it an active analyst: it compares the CURRENT period to the immediately-prior one across all four pillars (rank, traffic, AI, conversions) and emits a prioritized list of plain-English alerts plus the single most important thing to do this week. RULES-BASED (no server-side LLM, consistent with the no-training architecture); the user's own LLM narrates. A SYNTHESIS over data s33k already has, not new collection, and honest where a signal has no baseline.
- [x] **`utils/analyst.ts`: a pure, exhaustively-tested change-detection engine.** `detectChanges(current, prior)` is a function only of its two `PeriodData` arguments: no DB, no network, no LLM, no clock, no randomness. Four per-pillar detectors, returns `{ alerts[], topPriority }` sorted highest-signal first (severity, then a stable rank/traffic/ai/conversions tiebreak). Thresholds are named constants: RANK crossing page one = high, a >= 5 same-side move = medium, newly-ranked/newly-dropped flagged by where they sit (and NO alert when there is no prior reading); TRAFFIC change >= 25% medium / >= 50% high, only off a non-zero prior; AI a brand-NEW referring engine = high (the one intentional "from nothing" rule, a real leading AEO signal), existing engine +/- 30% = medium; CONVERSIONS form-submission change >= 30% (drop = high, rise = medium). HONEST: a signal with no baseline produces no alert rather than a swing from zero; a quiet period returns `[]` + `null` topPriority.
- [x] **`GET /api/alerts?domain=&period=`.** Ownership-gated exactly like scoreboard.ts / briefing.ts (`authorize()` 401, non-GET 405, missing domain 400, `Domain.findOne({ where:{ domain, ...scopeWhere(account) }})` 403 BEFORE any read). REUSES the existing pillar reads, never duplicates: Keyword rank history (per-window position via an explicit `[start, end)` helper), the analytics provider `getSummary`/`getReferralSources` (prior window = doubled-minus-current for additive totals, since the provider only takes a relative period string), and S33kEvent (`type:'form_submit'`) for conversions over exact current/prior windows. NEVER 500s: every read is `.catch()`-wrapped to degrade per pillar, plus a last-resort try/catch returning an empty 200. Returns `{ alerts, topPriority, period, comparedTo, generatedFor, dataAvailability, error }`; `dataAvailability` is an honest per-pillar note when a signal had no baseline. Whitelisted `GET:/api/alerts`.
- [x] **The `alerts` MCP tool (39th) + required knowledge entry.** Rich self-explaining description framing it as the period-over-period "what changed and what to do" standup, naming the thresholds, the leading-AEO new-engine signal, topPriority, the dataAvailability honesty, and the never-500 contract. Added the `alerts` `CapabilityEntry` to `utils/knowledge.ts` (the knowledge-coverage / WF7 test fails the build otherwise); coverage green.
- [x] **MCP now exposes 39 tools.** Live `tools/list` over stdio returns 39 with `alerts` present; banner reads "39 tools and 5 resources registered."
- [x] **Test suite (2 new suites / +44 tests, full suite green at 52 suites / 502 tests).** `__tests__/utils/analyst.test.ts` (23: every rank/traffic/ai/conversion threshold and no-fire, the silence-on-no-baseline honesty, prioritization + topPriority derivation) and `__tests__/pages/alerts-route.test.ts` (21: the ownership gate with an assertion NO pillar read runs on a 403, the doubled-minus-current prior derivation, the new-engine vs zero-baseline distinction, and per-pillar graceful degradation when each read throws). The prior 458 untouched and still green.

## STRING -> TEXT Postgres schema-correctness fix (the rank-tracking persistence bug). DONE (full suite green)
The confirmed prod bug: sequelize `DataType.STRING` maps to `VARCHAR(255)` on Postgres but to unlimited TEXT on SQLite. Every STRING column that can hold a JSON blob, free text, a long URL/path, or any unbounded value silently broke on Postgres with real data (`value too long for type character varying(255)`) while passing every test, because the suite runs on SQLite where VARCHAR length is not enforced. The fatal case: `keyword.lastResult` (the 100-result SERP JSON payload) and `keyword.history` overflowed VARCHAR(255), so the SERP scrape could not write and rank tracking, the core feature, never persisted on Postgres.
- [x] **Audited every model under `database/models/` and widened the 21 at-risk columns to `DataType.TEXT`.** keyword: `keyword`, `domain`, `history`, `url`, `target_page`, `tags`, `lastResult`, `lastUpdateError`, `settings` (9). domain: `domain`, `tags`, `notification_emails`, `search_console`, `scrape_strategy`, `subdomain_matching` (6). crawler_hit: `path`, `userAgent` (2). s33k_event: `page`, `label`, `selector` (3). waitlist: `note` (1). This fixes FRESH deploys on both dialects. Genuinely-short fixed fields (timestamps, enums, slug, UUID `umami_website_id`, `key_prefix`, the 64-char `key_hash`, hostnames-as-identifier, `device`/`country`/`city`, `session`, the `source` classification label) deliberately stay STRING. `feature_request.request`/`context` were already TEXT.
- [x] **One idempotent, Postgres-guarded migration: `1750147200012-widen-string-columns-to-text.js`** (next number after `...011`). Gated on `getDialect() === 'postgres'` (full no-op on SQLite, which shares TEXT affinity and enforces no length). For each of the 21 columns it `describeTable`-guards table+column existence (safe on a fresh or partial DB) and runs `ALTER TABLE "<t>" ALTER COLUMN "<c>" TYPE TEXT` wrapped in per-column try/catch, so a re-run is a clean no-op and a single failure never aborts the rest. Non-destructive widen, no data loss. No `down` (narrowing back to VARCHAR(255) would risk truncating the very data this exists to keep). Supports both the Umzug v3 `{ context }` and classic `(queryInterface, Sequelize)` conventions. The model change + the migration together fix both fresh deploys and the existing prod DB on the next deploy.
- [x] **Verification (all green).** `npm run lint` clean; `npm run build` Compiled successfully; `npx jest --ci` **54 suites / 533 tests pass** (the SQLite suite is unaffected by the TEXT change). Reproduced the bug on a real throwaway local Postgres (a 300-char insert rejected pre-migration), confirmed post-migration the columns are `text` and a 5000-char SERP payload persists, and a second `up()` is a clean no-op. Em-dash scan (U+2014) on all changed files: 0. Secret scan clean. Longest migration line 110 (under 150). `.env` and the running dev server untouched.

## Post-week (beyond Fork Week)
- [ ] Point getmasset.com at autocapture: swap the plain Umami snippet for the s33k autocapture snippet (`<script defer src=".../s33k.js" data-domain="getmasset.com">`) so engagement events start flowing. A deploy/site step, human-approved, not a build blocker.
- [ ] Migrate s33k's own DB from SQLite to the same Postgres (one owned stack)
- [ ] s33k.io landing page
- [ ] Build per-seat billing (deferred; the invite system is the seat substrate)
- [ ] Design pass on the invite-accept and waitlist pages (currently functional-minimal)

---

## Starter keyword set (kept small to stay on the free tier)

Strategic, winnable, on-positioning picks. These are intent + winnability + alignment calls, not volume data; once Google Search Console is connected we will see real impression data and refine.

| Keyword | Target page | Why it is impactful |
|---|---|---|
| masset | / | Brand baseline. Should be #1; catches aggregators or competitors outranking the brand term. |
| AI-ready DAM | /digital-asset-management-software | The core differentiated positioning. Low competition, ownable, defines the category Masset wants. |
| DAM MCP server | /software/mcp | Masset's unfair advantage. One of the only DAMs with an MCP server; genuinely winnable, on-trend. |
| AI digital asset management | /digital-asset-management-software | Category term with AI intent; aligns with positioning; winnable mid-tail. |
| Highspot alternative | /compare/masset-vs-highspot | Highest commercial intent. Biggest sales-enablement name; searchers are in-market. |
| Seismic alternative | /compare/masset-vs-seismic | Same logic, second-biggest name. |
| how to make website AI readable | /resources/blog/how-to-make-website-ai-readable | Strong existing pillar article; GEO-aligned, very winnable long-tail; proves content ranking. |

Expand once the scraper is confirmed free and the per-page view exists.

---

## Multi-tenant build (decisions LOCKED with Ben 2026-06-17)

The foundation is already shipped and flag-gated: `MULTI_TENANT` env (default off), `scopeWhere(account)`,
`ownerIdFor(account)`, `owner_id` columns, and GET-only member API keys. This section records the
product-shape decisions that turn the foundation into a real friends-sign-up product. Build in progress.

**The four locked decisions (Ben chose all recommended options):**
1. **Signup: invite-only.** No public self-serve signup. Ben/admin creates accounts or sends invite links
   (the existing Invite/Waitlist models are the substrate). Keeps SERP/COGS and abuse risk near zero early.
2. **Login: email magic link** (passwordless, via Resend, which is already wired). No password storage.
   Each verified email maps to an Account. Web login is for managing the account + getting the API key +
   managing domains/sharing; MCP/API access stays on per-account API keys (pasted as `S33K_API_KEY`).
3. **Sharing: per-domain, read-only.** A domain owner invites a collaborator (by email) to ONE site; the
   collaborator gets read-only access to that domain (reuses the GET-only member-key semantics). Like
   sharing a single Google Analytics property, matching Ben's "set it up for a site and share it" wording.
   Implication: reads must resolve "owned OR shared-to-me" domains, not just `owner_id = me`; writes stay
   owner-only. A new per-domain access/share table is the additive layer on top of `owner_id`.
4. **Scope: isolation + sharing + safety caps. Billing DEFERRED.** Wire tenant isolation, the sharing flow,
   and sane per-tenant keyword + refresh-cadence caps to bound the shared Serper cost. No Stripe/plans yet.

**Defaults Ben delegated (not separately asked):** shared Postgres + `owner_id` scoping (the built design);
each tenant = an Account with its own API key; Resend for invite + magic-link emails; the `domain` column
stays globally unique (one owner per domain, shared collaborators read via the share table).

**Security bar:** a cross-tenant read/write leak is the one unforgivable bug. This build gets a dedicated
isolation-focused adversarial review before deploy, on top of the normal gate.

### What already exists (foundation map, 2026-06-17) and what is left

ALREADY BUILT + FUNCTIONAL (do not rebuild): `MULTI_TENANT` flag (default off, exact `=== 'true'`),
`resolveAccount`/`authorize` (legacy global APIKEY always resolves to admin ID 1; per-account keys behind
the flag with role admin/member; member = GET-only enforced in authorize), `scopeWhere`/`ownerIdFor`
(tested), `owner_id` on domain/keyword/s33kEvent/goal/segment/featureRequest (CrawlerHit scoped by the
globally-unique domain, no owner_id), and the Account/ApiKey/Invite/Waitlist models. Functional routes:
`account` (admin create/list), `account-key` (mint/revoke), `invite` (external quota-gated + internal
member, atomic single-use accept, rate-limited), `waitlist`, `me`, `export` (scoped, secret-stripped),
`account-data` (scoped irreversible hard-delete). `utils/send-invite.ts` sends via Resend (best-effort).
Tests: `scope.test.ts`, `resolveAccount.test.ts`. NO frontend account/key/team UI. Web login is still
legacy single-admin JWT (USER/PASSWORD/SECRET).

EXECUTION PLAN (sequenced; route-heavy waves run AFTER the dashboard wave to avoid file collisions):

- **M1 Isolation completeness (security-critical, do first).** Audit EVERY `pages/api/*` data route. Wire
  `scopeWhere(account)` into every Keyword/Goal/Segment/S33kEvent query and `ownerIdFor(account)` on every
  create. For per-domain routes adopt the access-helper (see M2): verify the caller can reach the named
  domain before any pillar read. Close the gaps the map flagged (keywords, goals, segments, and any
  analytics route lacking the `Domain.findOne({domain, ...scopeWhere})` gate). Add isolation tests:
  member-key write rejection, and two-account cross-tenant leak (account A cannot read/mutate B's domain,
  keywords, goals, segments, events). This is what makes flipping the flag safe.
- **M2 Per-domain read-only sharing (the share feature). REVISED to the scoped-key approach (2026-06-17),
  which is far lower risk than a cross-account DomainShare model.** The cross-account model would force a
  re-refactor of ~47 routes' pillar queries (a shared viewer is a different account, so `scopeWhere(viewer)`
  filters the OWNER's data to zero rows). Instead: a "share" is a read-only API key minted on the OWNER's
  account but RESTRICTED to one domain. Because the key lives on the owner's account, `scopeWhere(owner)`
  and every pillar query already work UNCHANGED (no route refactor); the only new enforcement is the domain
  restriction. Implementation:
  - ApiKey gains a nullable `scoped_domain` (TEXT) column (fail-loud idempotent migration). A normal key
    has it null; a share key has it set to the one domain it may read.
  - `resolveAccount` surfaces the resolved key's `scoped_domain`; `authorize()` enforces it centrally: a
    scoped key is member/GET-only (already) AND the request MUST target that exact domain
    (`req.query.domain === scoped_domain`), else 403. Routes with no domain param (portfolio, domains list,
    account, me) carry no matching domain, so a scoped key is denied there. This is the single chokepoint:
    a share key can read exactly one domain's per-domain routes, nothing else.
  - Share flow + MCP tools: `share_domain` (owner mints a read-only key scoped to a domain it OWNS, optional
    Resend email to the collaborator), `list_domain_shares` (the owner's scoped keys), `revoke_domain_share`
    (revoke one). Reuses the account-key mint machinery + the `scoped_domain` field + `send-invite`.
  - Tests: a scoped key reads its domain, gets 403 on any OTHER domain, 403 on any write, 403 on portfolio /
    domains / account (no-domain routes); the owner can mint/list/revoke; a non-owner cannot share a domain.
  The `resolveDomainAccess` chokepoint from M1 stays owner-only and unchanged (sharing is enforced one level
  up, in authorize, by the scoped-key restriction), so M1's verified isolation is untouched.
- **M3 Per-tenant safety caps.** Per-account keyword cap (e.g. default 50, from Account.plan or a constant)
  enforced on add_keyword; refresh-cadence cap enforced on the refresh path. Honest 4xx with the limit when
  exceeded. Admin (ID 1) is uncapped. Bounds the shared Serper COGS.
- **M4 Magic-link login + minimal account UI.** Email -> signed one-time token (SECRET) -> session cookie ->
  resolveAccount maps the session to that email's Account (extend the cookie path which today only yields
  admin). Resend sends the link. A login page, and an account page (see your domains, copy your API key +
  MCP config, mint/revoke keys, share a domain + see who it is shared with). The invite->key->MCP path
  already works without this, so M4 is the management layer, lower priority than M1/M2.
- **Then:** dedicated isolation adversarial review across M1-M4, full gate, deploy. FLAG FLIP (`MULTI_TENANT=true`
  on prod) is the launch switch: do it only AFTER the isolation review is clean, ideally verified with a
  throwaway second test account proving it cannot see getmasset.com. Leaning toward leaving the actual prod
  flip for Ben to greenlight, or doing it with a test-tenant verification and immediate rollback path.
