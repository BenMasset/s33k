USER=admin
PASSWORD=0123456789
SECRET=4715aed3216f7b0a38e6b534a958362654e96d10fbc04700770d572af3dce43625dd
APIKEY=5saedXklbslhnapihe2pihp3pih4fdnakhjwq5
SESSION_DURATION=24
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Analytics provider (per-page traffic for the scoreboard join) ---------
# s33k OWNS its analytics via self-hosted Umami (open source, MIT). Umami is the
# standalone, owned analytics engine; Lodd is a legacy/dev option (closed SaaS).
# ANALYTICS_PROVIDER selects the backend: "umami" (default) or "lodd".
ANALYTICS_PROVIDER=umami

# Umami (self-hosted v2): the standalone, owned analytics engine.
# UMAMI_BASE_URL: base URL of your self-hosted Umami instance (with or without /api).
# UMAMI_WEBSITE_ID: the Umami website id to read. If unset, s33k looks it up from
#   GET /api/websites by matching the domain.
# Auth: provide EITHER UMAMI_API_KEY (a pre-issued bearer token) OR
#   UMAMI_USERNAME + UMAMI_PASSWORD (s33k logs in via POST /api/auth/login).
# UMAMI_METRICS_TYPE: page-grouping metric, "url" (default) or "path".
# UMAMI_BASE_URL=https://analytics.example.com
# UMAMI_WEBSITE_ID=
# UMAMI_API_KEY=
# UMAMI_USERNAME=
# UMAMI_PASSWORD=
# UMAMI_METRICS_TYPE=url

# Lodd Analytics (legacy/dev provider; closed third-party SaaS). Optional.
# Used only when ANALYTICS_PROVIDER=lodd.
# LODD_API_KEY is your Lodd API key. LODD_SITE is the site UUID to read traffic for.
# LODD_BASE_URL defaults to https://api.lodd.dev/v1 when unset.
# LODD_API_KEY=
# LODD_SITE=
# LODD_BASE_URL=https://api.lodd.dev/v1