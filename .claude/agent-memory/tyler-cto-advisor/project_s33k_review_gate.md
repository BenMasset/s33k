---
name: s33k-review-gate
description: Recurring patterns the s33k Tyler-gate review checks, and what has shipped clean vs been caught
metadata:
  type: project
---

s33k (`/Users/ben/Projects/s33k`) runs a mandatory CTO review gate before every `railway up` (repo `CLAUDE.md` section E). I am that gate.

**Why:** s33k ships fast via parallel AI agents; green tests are necessary but not sufficient. The high-value defect classes on this codebase are cross-tenant leaks, auth/scope gaps, data-loss migrations, and stale product-fact claims.

**How to apply (the load-bearing checks, in order):**
- Multi-tenant scoping: every per-domain route must `authorize()` then spread `scopeWhere(account)` into EVERY query, and gate ownership via `resolveDomainAccess(account, domain)` (returns null = deny, treat as 403) BEFORE any read. The `domain` column is globally `@Unique`, so by-domain scoping cannot leak across tenants.
- Scoped SHARE keys: enforced in `utils/authorize.ts` lines ~44-55. A share key is allowed ONLY when method is GET + route is in `scopedKeyAllowedRoutes` + canonical `?domain=` equals canonical `scoped_domain`. A missing/empty domain canonicalizes to `''` and is DENIED. This means any "no-domain / list-my-domains" branch in a route is structurally unreachable for a share key. New authed routes need entries in BOTH `allowedApiRoutes` and (if share-key-safe) `scopedKeyAllowedRoutes`.
- A not-owned domain answered as a generic 200 (instead of 403) is safe IFF no tenant data is read on that path (counts guarded by `owned ?`) and the response is identical for "does not exist" vs "owned by another tenant" (no existence oracle). `start_here` does this correctly.
- MCP tool count integrity: runtime banner is computed `70 + adminToolsRegistered` (NOT hardcoded total). Customer tools register via `server.registerTool` directly (in the 70 base); admin tools via `registerAdminTool`/`countingRegister`. New tool must touch: `mcp/src/tools.ts`, `utils/knowledge.ts` (knowledge-coverage jest guard parses tools.ts and FAILS the build if missing), `mcp/smoke-test.mjs` EXPECTED_TOOLS, `__tests__/pages/hosted-mcp-scope.test.ts` count. The coverage guard has no hardcoded total, so the count cannot silently rot, but doc quick-map lines (CLAUDE.md / AGENTS.md) DO go stale and are a recurring should-fix.
- SSRF (`utils/site-crawl.ts`): the pinned-dispatcher pattern must hand back ONLY the single pre-vetted IP for BOTH dns.lookup callback shapes (`{all:true}` array form and 3-arg form), and `safeFetchText` must re-validate + re-pin + close the dispatcher PER redirect hop. Verify the rebinding property survives any edit here.
- Em dashes: grep U+2014, count must be zero, everywhere including code/comments.

**2026-06-19 review (`s33k-tyler-followups`):** SSRF lookup-shape fix + new `start_here` MCP tool (82 total / 70 customer) + setup-checklist dedup (`computeSetupState` now shared). Verdict SHIP. Only findings: two stale `(81 + 5)` quick-map lines in CLAUDE.md:312 and AGENTS.md:150 (cosmetic). Gate green on Node 20: lint clean, mcp build clean, Next "Compiled successfully", 1156/1156 jest.

**2026-06-19 review (`s33k-phase2-analyst-home`):** Pure enhancement to `utils/analyst.ts` (the rules-based proactive analyst). Two new detectors, NO new tool (count stayed 82/70/12). Verdict SHIP, zero findings. The change was uncommitted working-tree-only (HEAD == main); review target was the working tree. New detector-correctness checks worth reusing for any analyst.ts edit: (1) the analyst is a PURE function (no imports/fetch/clock/random) and MUST stay so, since "no server-side LLM" is a SECURITY.md trust fact verified by `security_facts`; grep analyst.ts for `import|fetch(|Date.now|Math.random` = must be empty. (2) New alert rules ordered before a broader rule must `return` to avoid double-firing one signal (the AI-collapse branch correctly sits after the 30% gate, before the medium-shrink push, and returns). (3) Any rate/ratio detector must guard the denominator with a meaningful-sample floor (here `priorVisitors >= 20 && prior > 0 && currentVisitors > 0`) before dividing. (4) Never-500: both `pages/api/alerts.ts` and `pages/api/daily-brief.ts` run the pure detector inside a try with per-provider `.catch` fallbacks + a last-resort 200 catch, and gate ownership via `resolveDomainAccess` 403 BEFORE any read. (5) When a tool DESCRIPTION claims a new behavior ("surfaces collapsed engines / conversion rate"), verify the route actually threads the inputs (here `traffic.visitors` is now passed to `detectConversionChanges` in both routes) and that `composeDailyBrief` consumes `analyst.alerts`, so the claim is code-backed not aspirational. Co-firing alerts (volume drop + rate drop on the same regression) are intentional and honest, not a bug; the existing `6/10` volume test was correctly updated to expect the rate alert co-firing. Gate green on Node 20: lint clean, mcp tsc clean, Next "Compiled successfully", 1165/1165 jest, zero U+2014, no conflict markers.

**2026-06-19 review (`s33k-phase3-perpage-moat`):** Per-page cross-pillar moat made EXACT. `entry-pages.ts` + `scoreboard.ts` now derive per-page AI-landing counts from sessionized first-party `S33kEvent` rows (channel==='ai', grouped by landing page) instead of the provider's never-populated `landing_path`; scoreboard gains optional `goal`/`goalId` -> per-page `conversions` + `conversionRate`. New shared pure helper `utils/ai-landing.ts` (`aiLandingFromSessions`, takes already-scoped rows, never queries DB). No new tool (82/70/12 intact; tools.ts/knowledge.ts changes were prose-only). Verdict FIX-THEN-SHIP, ONE should-fix. Tenant scoping clean: both new `S33kEvent.findAll` and the `Goal.findOne` spread `...scopeWhere(account)` and run AFTER `resolveDomainAccess` 403. Correctness clean: AI/conversion maps keyed by `cleanPath(s.landingPage)` which matches the provider's `pathClean` (also `cleanPath`), so the join lines up; human-only + pageviewCount>0 filter applied; conversionRate guards divide-by-zero (null when no landing sessions). **The one finding (should-fix, never-500):** goal resolution (`Goal.findOne` at scoreboard.ts ~L118) was hoisted ABOVE the try (opens L130) to make the 400/404 validation returns win. But the sibling prior-art route `entry-page-report.ts` resolves its goal INSIDE its try and its explicit 400/404 returns still fire fine, proving the hoist was unnecessary, and it cost the never-500 net: a transient `Goal.findOne` THROW (DB blip / the exact column-case class in CLAUDE.md B) now escapes to a Next 500 instead of degrading. Not a leak (correctly scoped+gated), purely robustness. Fix: move the goal block inside the existing try, or wrap it. Recurring lesson: an early `return res.status(4xx)` INSIDE a try is the right pattern here, do not hoist a DB read out of the try to "protect" a validation return. Gate green on Node 20: lint clean, mcp tsc clean, Next "Compiled successfully", 1175/1175 jest (incl. new `scoreboard-conversions` + updated `entry-pages`/`scoreboard-ai-attribution`), zero U+2014, no conflict markers.

**2026-06-20 review (allowlist widen + first-party sessionize, two findings):** Verdict SHIP, zero findings. (#7, HIGH) added `GET:/api/weekly-digest`, `GET:/api/executive-summary`, `GET:/api/competitor-visibility` to `scopedKeyAllowedRoutes` so a read-only per-domain SHARE key can call them. Verified each of the three follows the per-domain gate byte-for-byte: reads `q.domain` (400 if missing), `resolveDomainAccess` 403 BEFORE any pillar read, every `S33kEvent`/`Keyword`/`Goal` query spreads `...scopeWhere(account)` + keyed on the one domain, report-cache key built AFTER the gate and prefixed with resolved account ID (HIT cannot cross tenants). `competitor-visibility` surfaces OTHER ranking domains read from the CALLER'S OWN tracked keywords' stored `lastResult` SERP arrays = public Google ranking URLs, NOT another tenant's private rows = NOT a leak. (#8) `insights.ts` + `briefing.ts` now fetch + sessionize first-party `S33kEvent` to feed `estimateHumanTraffic` (4th arg), replicating `human-traffic.ts`; both spread `...scopeWhere(account)` + run AFTER `resolveDomainAccess`. Data-honesty win confirmed: briefing headline `humanCount` (briefing.ts ~L419) uses `estHumanVisitors` when `estVisitors>0`, falls back to `summaryData.visitors` only when the estimate is genuinely unavailable; insights bot finding gates on `estVisitors>0` so it reports the real split not a false 0-bots. Flag-safe (MULTI_TENANT off => scopeWhere {} => prior behavior). **One noted asymmetry (leave-it):** briefing wraps its `S33kEvent.findAll` in `.catch(()=>[])` (degrade one signal); insights does not (relies on outer try => 400s whole route on DB error). Deliberate, matches each route's existing error posture; mirror the `.catch` only if you later want insights to degrade as gracefully. Gate green: lint clean, Next build clean, 1226 jest, zero U+2014. **Pattern now 5 straight s33k gates: the isolation seam (scopeWhere + resolveDomainAccess-before-read + globally-@Unique domain) has held EVERY time; the gate's value is confirming gate-ordering byte-for-byte on the newly-exposed routes, plus catching small non-load-bearing periphery (the phase3 hoist, the stale quick-map counts). Keep running it on every parallel-agent build before deploy.**

**2026-06-22 review (operator-data-isolation, FIX-THEN-SHIP):** The biggest isolation change yet. With `MULTI_TENANT` on, `scopeWhere(operator id=1 / null account)` now returns `{ owner_id: null }` (the legacy null-owner partition) instead of `{}`, so the operator's everyday admin key/cookie/hosted-MCP key is no longer a master reader of every tenant's content. Verified the linchpin: `resolveDomainAccess` spreads `scopeWhere` into the canonical `Domain.findOne`, so a tenant-owned domain (owner_id=tenantID) resolves to no row = 403 for the operator. The ONE operator-wide read (cron SERP sweep) is the named, single-call-site, `isAdminAccount`-gated, audit-logged `unscopedOperatorWhere()` = `{}`; spend-brake still keys on `Object.keys(scope).length===0` and fires correctly for both paths. Also: account.email now cryptr ciphertext + deterministic keyed-HMAC `email_hash` blind index (lookup/dedupe by hash, never ciphertext); new AuditLog table + best-effort `recordAudit` (no-op flag-off). Isolation seam held for the 6th straight gate. **MUST-FIX caught (blocks deploy):** migration `1750147200027-encrypt-account-email.js` raw SQL references the account PK as lowercase `id` in 5 places (lines 91/112/123/156/166), but the column is uppercase `ID` (create-account migration line 36; the seed migration line 70 correctly uses `WHERE ID = 1`). On Postgres unquoted `id` != `"ID"` so the backfill SELECT/UPDATE throw `column "id" does not exist`; with fail-loud entrypoint that REFUSES THE CONTAINER BOOT on any populated instance with a non-null email. Jest is green because it runs on SQLite (case-insensitive) = the exact test-path-vs-prod-path trap that hid the VARCHAR(255) and UmamiProvider bugs. Fix = `id` -> `"ID"` in those 5 column references (the `:id` param name is fine), re-gate, repro against real Postgres with a seeded email row. Should-fix: stale `utils/scope.ts` header comment (lines 4-7) still says "Nothing in this file is wired into any route yet" though 123 call sites now spread it (stale comment on the security seam). **Recurring lesson reinforced: raw-SQL column identifiers in migrations must byte-match the created column case; SQLite-backed jest will never catch a Postgres case mismatch. Grep every new migration's raw SQL for the account PK = must be `ID`/`"ID"`, never `id`.**

**2026-06-22 review (scale-hardening round 2, SHIP):** The auth+billing+migration trifecta. Verdict SHIP, zero must-fix, two should-fix (both leave-it-for-now). Touched: shared-store rate limiter (`utils/rate-limit-store.ts` + `rateLimitAsync` front door + `rate_limit` table/model/migration), atomic caps (`utils/caps-guard.ts` row-lock reserve), bounded scrape concurrency (`utils/scrape-queue.ts` + refresh.ts), cron paging, collect.ts bulkCreate, DB_POOL_MAX, last_used_at throttle, two new migrations (029 create-rate-limit, 030 scale-indexes). **Prod config that scoped the review: `MULTI_TENANT=true`, all five new scale envs UNSET (RATE_LIMIT_BACKEND/SCRAPE_CONCURRENCY/CRON_PAGE_SIZE/DB_POOL_MAX/LAST_USED_THROTTLE_MS) so defaults apply.** So the live deltas for getmasset are only (a) scrape now bounded at 10 concurrent and (b) collect uses bulkCreate; the Postgres rate-store is DORMANT (backend defaults to memory = byte-for-byte the old in-memory `rateLimit()` wrapped in a resolved promise). **Verifications that passed:** caps lock is a real `LOCK.UPDATE` (SELECT FOR UPDATE) on the Account row inside one transaction, re-count-then-create, so the count-then-create TOCTOU is closed on Postgres MVCC (not just SQLite-serializes-writes); `connection.models.Account` IS populated in prod so `lockAccountRow`'s silent-return guard never fires; admin sentinel (ID 1 = getmasset) hits `isUnlimited`=true = pure passthrough no-lock. `runWithConcurrency` exactly-once (no double Serper charge / no dropped keyword) + input-order + throw-isolation all pinned by test; shared-cursor `next++` has no await between read and increment so single-threaded JS can't double-claim. collect.ts: PII-strip intact (`sanitizeBatch` runs BEFORE row build, rows mapped from `clean`), owner_id stamped on every row incl webvitals, `bulkCreate({validate:true})` matches old per-row `create` validation, bulk-fail falls back to per-row skip-and-continue, `storeRows` never throws and sits inside the route's outer try, recorded===0 total-failure 500 preserved. All five new index columns (session/domain/type/created on s33k_event, stripe_customer_id on account) byte-match lowercase create-table migrations; rate_limit "key"/window_start/count byte-match model; "key" always quoted (reserved word); BIGINT window_start coerced with Number() (pg int8->string). Migrations fail-loud + idempotent (describeTable/showIndex probes are the only swallows, createTable/addIndex unwrapped). Prior MUST-FIX (migration 027 `id` vs `"ID"`) confirmed already fixed + committed in d74c5c0, out of scope. Gate green Node 20: lint clean, build "Compiled successfully", 153 suites / 1341 jest, zero U+2014, no host leak. **SHOULD-FIX (leave-it): (1)** the Postgres `INSERT ... ON CONFLICT ("key") DO UPDATE ... RETURNING ... ` under `QueryTypes.SELECT` is the FIRST use of that pattern in the repo and is NOT exercised by any test (the cross-instance test runs the SQLite UPSERT-then-SELECT branch). It's known-good Sequelize behavior and the limiter FAILS OPEN on any store error, so a Postgres-path bug degrades to un-braked, never a 500/lockout, AND the whole store is dormant until someone sets RATE_LIMIT_BACKEND=postgres. Before flipping that env in prod, repro the RETURNING path against real Postgres (the exact test-path-vs-prod-path class from the VARCHAR/UmamiProvider bugs). **(2)** cron now reads ONE bounded page (500, ID-ASC) then spend-brakes; at >500 keywords on an instance, keywords past ID-500 are not scraped that tick, and an inactive-tenant-heavy low-ID range can starve due active keywords past the page. Fine + correct at current scale (getmasset ~34 kw, all active operator-owned) and the code HONESTLY documents it as a single-pass-for-safety follow-up; the hard blocker (50k simultaneous calls) is removed. Build the resumable cursor/tick split before onboarding a tenant with >500 keywords or many tenants. **Pattern now 6 straight gates: isolation seam held; the gate's value this round was confirming the billing lock is a REAL FOR UPDATE on Postgres (not SQLite-serializes-by-accident) and that the dormant-vs-live env split means the scariest change (the rate store) isn't even active in prod.**

See [[project_s33k_codebase_review]] for the original V1 codebase review.
